> **本文件方向已作废（勿据此开发）。** 文中"重构 chat 云函数做网关 / 新增 relay 云函数 / 在 habit 做用量看板 / key 签发系统 / OpenAI 兼容接口"等描述均错误——这些能力其实已在 **WorkTrace Relay 独立服务**里落地，不在 habit 仓库。正确认知请看同目录 `AI中转站统一定位说明.md`。habit 侧唯一要做的是把 `cloudfunctions/chat/index.js` 改为 HTTP 调用 Relay 网关 + Habit tab 签发的 key。

# habit 小程序 AI 中转站开发交接文档（历史归档，内容作废）
## 一、项目背景与需求
### 项目现状痛点
当前 habit 小程序的 AI 对话功能**完全硬编码对接火山引擎 ARK API**，存在以下核心问题：
1.  **单一服务商绑定**：仅支持火山方舟，无法灵活切换到其他 AI 服务商（如腾讯混元、阿里通义千问等）
2.  **无容错机制**：当火山账号欠费、服务不可用或 API 密钥失效时，整个 AI 对话功能直接崩溃
3.  **缺乏统一管理**：没有统一的调用入口，难以新增模型、调整参数或监控调用情况
4.  **配置硬编码**：模型 ID、接口地址均写死在代码中，无法通过配置动态修改

### 开发目标
开发 **AI 调用中转站**，实现：
- 统一的 AI 服务调用入口，支持多服务商动态切换
- 灵活的配置管理，无需修改代码即可更换 AI 模型和服务商
- 内置熔断与降级机制，提升系统稳定性
- 完整的调用日志与监控，方便排查问题
- 兼容现有 AI 对话功能的交互逻辑，降低迁移成本

## 二、现有 AI 接入代码现状
### 核心硬编码问题（`cloudfunctions/chat/index.js`）
1.  **固定火山方舟配置**：
    - 固定接口地址：`https://ark.cn-beijing.volces.com/api/v3/chat/completions`
    - 默认模型 ID：`ep-20260604101325-f2wcq`
2.  **仅依赖火山 API 密钥**：
    - 仅读取 `ARK_API_KEY` 环境变量，无其他服务商支持
3.  **无容错处理**：调用失败直接返回错误，无备用方案

### 前端调用链路（`utils/api.js`）
前端通过统一的 `sendChat` 方法调用 chat 云函数，现有逻辑无需修改即可兼容中转站架构，仅需确保云函数返回格式一致。

## 三、AI 中转站架构设计
### 整体架构分层
```
前端页面 → 统一调用入口（重构后的 chat 云函数） → AI 适配层 → 具体服务商 API → 返回结果
```
1.  **调用层**：兼容现有前端调用逻辑，保持 `sendChat` 接口不变
2.  **适配层**：为不同 AI 服务商提供统一的调用接口，处理参数转换、错误处理
3.  **配置层**：通过环境变量或云函数配置动态读取服务商、模型、密钥等信息
4.  **监控层**：记录调用日志、错误统计、性能指标
5.  **降级层**：当主服务商不可用时，自动切换到备用服务商

### 核心流程
1.  前端调用 `sendChat` 方法，传入对话参数
2.  chat 云函数读取全局配置，选择当前启用的 AI 服务商
3.  AI 适配层将通用请求格式转换为对应服务商的 API 格式
4.  调用服务商 API 获取结果
5.  将服务商返回结果转换为通用格式，返回给前端
6.  记录调用日志，触发熔断/降级逻辑（如果需要）

## 四、核心功能需求
### 1. 多服务商支持
- 已实现：火山方舟（兼容现有代码）
- 待实现：腾讯混元、阿里通义千问、OpenAI GPT 等
- 支持通过配置动态切换默认服务商

### 2. 动态配置管理
- 支持通过云函数环境变量配置：
  - `AI_PROVIDER`：默认服务商（如 `volc_ark`、`tencent_hunyuan`）
  - `AI_API_KEY`：AI 服务商 API 密钥
  - `AI_MODEL`：默认模型 ID
  - `AI_ENDPOINT`：自定义接口地址（用于私有部署模型）
  - `AI_BACKUP_PROVIDERS`：备用服务商列表（逗号分隔）
  - `AI_TEMPERATURE`：默认生成温度（可选）
  - `AI_MAX_TOKENS`：默认最大 token 数（可选）

### 3. 统一请求/响应格式
- 适配层自动处理不同服务商的参数差异：
  - 火山：`model`、`messages`、`temperature`、`max_tokens`
  - 腾讯混元：`Model`、`Messages`、`Temperature`、`MaxTokens`
  - OpenAI：`model`、`messages`、`temperature`、`max_completion_tokens`
- 统一返回格式给前端：保持现有 `{ reply, action, quickReplies }` 结构

### 4. 熔断与降级机制
- 连续失败阈值配置（默认 3 次）
- 自动切换到备用服务商
- 服务恢复自动切回主服务商
- 降级时返回友好提示（不影响用户体验）

### 5. 监控与日志
- 记录每次调用的：
  - 服务商、模型、调用耗时
  - 请求参数、响应结果
  - 错误信息与堆栈跟踪
- 支持将日志写入云开发数据库或本地日志文件

### 6. 错误处理与兼容
- 兼容现有 AI 对话的交互逻辑和返回格式
- 处理不同服务商的错误码，转换为统一的错误提示
- 支持自定义参数透传，满足不同场景需求

## 五、具体开发任务清单
### 第一阶段：基础架构重构
1.  **提取 AI 调用模块**
    - 将 `cloudfunctions/chat/index.js` 中的火山调用逻辑提取为独立的 `ai-provider` 目录
    - 实现通用的 AI 调用接口，定义统一的请求/响应格式

2.  **新增配置管理模块**
    - 编写配置读取工具，从 `process.env` 读取全局配置
    - 支持配置校验，确保必填参数完整

3.  **实现火山适配层（兼容现有代码）**
    - 保留现有火山方舟的调用逻辑，作为默认适配模块
    - 确保与现有代码完全兼容，不破坏原有功能

### 第二阶段：多服务商适配
4.  **腾讯混元适配模块**
    - 对接腾讯云 ASR + 大模型 API（参考现有 stt 云函数的腾讯云配置）
    - 处理参数转换与错误处理

5.  **通用适配框架**
    - 抽象服务商适配接口，方便快速接入新的服务商
    - 实现参数自动转换工具类

### 第三阶段：稳定性与监控
6.  **熔断与降级逻辑**
    - 实现服务可用性检测
    - 实现自动切换备用服务商的逻辑
    - 配置化的熔断阈值与重试策略

7.  **调用日志模块**
    - 编写日志记录工具，将调用信息写入云开发数据库
    - 实现日志查询与统计接口（可选）

### 第四阶段：测试与部署
8.  **单元测试**
    - 编写各服务商适配模块的单元测试
    - 测试熔断与降级逻辑

9.  **集成测试**
    - 在本地开发环境测试全流程调用
    - 在云开发环境部署测试，验证兼容性

10. **文档与配置**
    - 编写部署文档，说明环境变量配置方法
    - 整理常见问题与排查指南

## 六、配置方案与环境变量
### 必选环境变量（chat 云函数）
| 变量名                | 说明                                                                 | 示例值                                  |
|-----------------------|----------------------------------------------------------------------|---------------------------------------|
| `AI_PROVIDER`         | 默认服务商标识                                                       | `volc_ark` / `tencent_hunyuan`        |
| `AI_API_KEY`          | AI 服务商 API 密钥                                                   | `ark-xxxxxx` / `AKxxxxxxxxxx`         |
| `AI_MODEL`            | 默认模型 ID                                                         | `ep-20260604101325-f2wcq` / `混元-lite` |

### 可选环境变量
| 变量名                | 说明                                                                 | 默认值                                |
|-----------------------|----------------------------------------------------------------------|---------------------------------------|
| `AI_ENDPOINT`         | 自定义接口地址（用于私有部署或非标准服务商）                           | 服务商默认地址                         |
| `AI_BACKUP_PROVIDERS` | 备用服务商列表（逗号分隔）                                           | 无                                    |
| `AI_TEMPERATURE`      | 默认生成温度                                                         | `0.8`                                 |
| `AI_MAX_TOKENS`       | 默认最大 token 数                                                   | `1024`                                |
| `AI_TIMEOUT`          | 调用超时时间（毫秒）                                                 | `45000`                               |

## 七、迁移实施步骤
### 步骤1：本地开发验证
1.  克隆现有代码，创建 `ai-provider` 目录
2.  重构 chat 云函数，提取通用调用逻辑
3.  测试火山服务商的兼容性，确保原有功能正常

### 步骤2：接入新服务商
1.  实现腾讯混元适配模块，使用腾讯云 API 密钥测试
2.  验证参数转换与返回格式是否符合要求

### 步骤3：配置熔断与降级
1.  实现服务检测逻辑，测试连续失败后的自动切换
2.  配置备用服务商，验证降级功能

### 步骤4：云开发部署
1.  将重构后的代码上传到云开发控制台
2.  配置云函数环境变量，测试线上功能
3.  验证前端调用是否正常，日志是否正确记录

### 步骤5：灰度发布与监控
1.  先在测试环境部署，邀请部分用户测试
2.  监控调用日志与错误率，确保稳定运行
3.  逐步全量发布，最终替代原有硬编码逻辑

## 八、测试与验证方案
### 1. 功能测试
- 验证原有火山方舟功能是否正常
- 验证切换到腾讯混元功能是否正常
- 验证不同参数（temperature、max_tokens）是否生效

### 2. 异常测试
- 模拟 API 密钥失效，验证错误处理是否正常
- 模拟服务商接口不可用，验证熔断与降级是否生效
- 验证日志是否正确记录所有调用信息

### 3. 兼容测试
- 验证前端 `sendChat` 调用无需修改即可正常工作
- 验证 AI 对话的交互逻辑（打卡、回复、快捷选项）与原有功能一致

## 九、遗留问题与注意事项
1.  **现有用户兼容**：迁移过程中需确保不影响现有用户的对话历史和使用习惯
2.  **密钥安全**：API 密钥需通过云开发环境变量安全存储，禁止硬编码到代码中
3.  **服务商差异**：不同服务商的参数格式、返回结果存在差异，需做好适配
4.  **监控告警**：建议新增云函数监控告警，及时发现服务异常
5.  **多语言支持**：如果需要支持多模态或其他语言能力，需在适配层中扩展

## 十、参考资料
1.  现有 chat 云函数代码：`cloudfunctions/chat/index.js`
2.  腾讯云混元 API 文档：https://cloud.tencent.com/document/product/1729
3.  火山方舟 API 文档：https://www.volcengine.com/docs/6460/117577
4.  微信云开发环境变量配置：https://developers.weixin.qq.com/miniprogram/dev/wxcloud/basis/environment.html