# habit 小程序：健身数据导入格式说明

这份文档给"生成数据的 AI"看。目标是产出一个 JSON 文件，通过小程序「我的 → 从剪贴板导入」一次性写入账号。

## 导入机制与最重要的前提

导入是**整包覆盖**，不是追加。`store.importData()` 会用你给的 JSON 替换掉 `habits` / `checkins` / `chatHistory` 三个数组（缺失就置空），`settings` 与默认值浅合并。

所以正确流程是：

1. 在小程序「我的」页点**导出数据**，得到当前账号完整 JSON（复制到剪贴板）。
2. 把这份导出 JSON 作为**基底**交给生成数据的 AI，让它在基底上追加历史数据，保留原有 habits 和 settings。
3. 拿到新 JSON，复制到手机剪贴板，在小程序里点**从剪贴板导入**。

如果直接给一份只含新数据的 JSON，现有习惯和打卡会被清空。

体积上限：整包 JSON 通过云函数上传，微信 event 参数上限 1MB，建议控制在 800KB 以内。`checkins` 不做裁剪，`chatHistory` 只保留最后 60 条，`taskLogs` 最后 150 条，`aiPlans` 最后 30 条。

## 顶层结构

```json
{
  "habits": [],
  "checkins": [],
  "chatHistory": [],
  "settings": {},
  "taskLogs": [],
  "aiPlans": [],
  "fitnessSeedVersion": 0,
  "version": 13
}
```

`taskLogs` / `aiPlans` 目前没有任何页面消费，保持空数组即可。`fitnessSeedVersion` 和 `version` 照抄。

## habits：习惯定义

一条健身习惯长这样：

```json
{
  "id": "h_strength",
  "name": "力量训练",
  "icon": "fitness",
  "color": "#6366f1",
  "frequency": "custom",
  "weekdays": [1, 3, 5],
  "reminder": "19:00",
  "plan": {
    "items": [
      { "text": "推日：卧推/肩推/三头", "color": "#6366f1" },
      { "text": "拉日：划船/引体/二头", "color": "#8b5cf6" },
      { "text": "臀腿日：臀推/腿举/硬拉", "color": "#ec4899" }
    ]
  },
  "createdAt": "2026-03-01"
}
```

| 字段 | 类型 | 要求 |
| --- | --- | --- |
| `id` | string | 全局唯一，`checkins.habitId` 要引用它。可读命名如 `h_strength` 即可，不必模仿系统生成的 `id_xxx` |
| `name` | string | **最多 20 字**，超出会被编辑页拒绝保存 |
| `icon` | string | 只能是这 16 个 key 之一：`fitness` `book` `water` `stretch` `music` `pen` `moon` `apple` `run` `palette` `note` `pill` `leaf` `sun` `target` `heart`。健身用 `fitness`，拉伸用 `stretch`，跑步用 `run` |
| `color` | string | 十六进制色值。建议取自调色板：`#6366f1` `#8b5cf6` `#ec4899` `#f97316` `#f59e0b` `#10b981` `#14b8a6` `#0d9488` `#3b82f6` `#ef4444` `#84cc16` `#eab308` |
| `frequency` | string | `daily`（每天）/ `weekdays`（工作日）/ `custom`（自定义星期） |
| `weekdays` | number[] | **仅 `frequency` 为 `custom` 时有意义**，取值 1-7，**1 = 周一，7 = 周日**。其他频率填 `[]` |
| `reminder` | string | `"HH:MM"` 24 小时制，不提醒填 `""` |
| `plan.items` | array | 可选的子项清单，打卡时可勾选。每项 `{ text, color }`，`text` 必填 |
| `createdAt` | string | `"YYYY-MM-DD"`。应当**不晚于**该习惯最早那条 checkin 的日期 |

## checkins：打卡记录（历史训练数据放这里）

一次训练记录长这样：

```json
{
  "id": "c_20260617",
  "habitId": "h_strength",
  "date": "2026-06-17",
  "note": "拉日。高位下拉 27kg×12×4；坐姿划船 25kg×12×3；直臂下压 15kg×12×3；二头弯举 7.5kg×12×3。拉伸：背阔靠墙下压 30秒×2、胸小肌门框 30秒×2。",
  "selectedItems": [
    { "text": "拉日：划船/引体/二头", "color": "#8b5cf6" }
  ],
  "createdAt": "2026-06-17T19:30:00+08:00"
}
```

| 字段 | 类型 | 要求 |
| --- | --- | --- |
| `id` | string | 全局唯一 |
| `habitId` | string | **必须**等于某个 `habits[].id`，否则这条记录不会显示在任何页面 |
| `date` | string | `"YYYY-MM-DD"`。**同一 habitId + 同一 date 只允许一条**，重复会导致连续天数和完成率计算异常 |
| `note` | string | 训练详情正文，日历和详情页会展示。建议单条 500 字以内 |
| `selectedItems` | array | 勾选了哪些计划子项，`{ text, color }`。`text` **必须与该习惯 `plan.items[].text` 完全一致**，否则回填时匹配不上。没有计划就填 `[]` |
| `createdAt` | string | ISO 时间字符串，详情页据此显示时刻。**必须带 `+08:00` 时区偏移**，写成 `Z` 结尾会导致显示时间差 8 小时甚至串到前一天 |
| `isMakeup` | boolean | **历史数据请不要设置或设为 `false`**，设为 `true` 时界面会给每条记录打"补卡"标记 |

连续天数（streak）按 `date` 逐日回溯计算，所以历史数据的日期必须真实——不要为了让数字好看而填连续日期。

## settings：个人设置与健身档案

```json
{
  "nickname": "圈圈",
  "avatar": "user",
  "wxNickname": "",
  "slogan": "稳住节奏就是赢",
  "aiTone": "sassy",
  "fitnessProfile": "身高163cm，当前体重59kg，目标57kg。训练4练1休...",
  "aiInputMode": "text",
  "reminderTimes": {}
}
```

| 字段 | 类型 | 要求 |
| --- | --- | --- |
| `nickname` | string | 100 字以内 |
| `slogan` | string | 1000 字以内 |
| `aiTone` | string | `normal` / `mild` / `sassy`（毒舌），三者之一 |
| `fitnessProfile` | string | **健身档案，20000 字以内**，AI 教练每次对话都会读它。见下节 |
| `aiInputMode` | string | `text` / `voice` |
| `reminderTimes` | object | 保持导出时的原值，不要自己造 |

### fitnessProfile 是体重和维度数据唯一的去处

数据模型里**没有**体重、围度、BMR、饮食这些结构化字段。这类信息只能作为纯文本写进 `fitnessProfile`，供 AI 教练阅读。建议按下面的分节组织，纯文本、无 markdown 表格（AI 读长文本时分节比表格更稳）：

```
【基础信息】身高163cm / 年龄xx / BMR约xxxx kcal
【体重轨迹】2026-03-01 61.2kg；2026-05-31 59.4kg；2026-08-07 58.9kg（晨重，空腹）
【围度轨迹】2026-05-31 腰围68cm 大腿52cm；...
【训练安排】4练1休，推/拉/臀腿/上肢循环
【生理周期】月初来月经，月中14-15号排卵，排卵日前后1-2天避开臀腿训练
【器械说明】插片式与加片式标重不可直接对比
【当前目标】...
```

体重数据请标注是晨重还是随机时间点——晨重反映前一天的训练效果，混在一起会让 AI 得出错误结论。

## chatHistory：教练对话

导入历史训练数据时**建议留空数组**。如果要写：

```json
{ "role": "user", "content": "今天练什么", "time": "2026-08-01T09:00:00+08:00" }
```

`role` 只能是 `user` 或 `assistant`，单条 `content` 3000 字以内，最多保留最后 60 条。

## 输出要求

- 一个 `.json` 文件，UTF-8，**不带 BOM**，顶层是对象不是数组。
- 不要有注释、不要有尾随逗号、不要用单引号——小程序端是严格 `JSON.parse`，任何语法错误会直接提示"不是合法 JSON"并终止。
- 不要输出 markdown 代码块包裹，直接给纯 JSON 内容，方便整段复制到剪贴板。
- 生成后自查三件事：所有 `habitId` 都能在 `habits` 里找到；同一 habitId 下 `date` 无重复；所有 `createdAt` 都带 `+08:00`。
