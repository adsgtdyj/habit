# 订阅弹窗测试指引 与 健身数据导入说明

## 一、订阅弹窗：真机没弹是代码 bug，已修

### 两种弹窗的机制

| 弹窗 | 谁弹的 | 出现条件 |
| --- | --- | --- |
| ①「让教练能提醒你」引导框 | 我们自己的 `wx.showModal` | 本地 Storage 里 `habit_subscribe_guided` 没值时。**一辈子只弹一次** |
| ② 微信原生订阅授权窗 | `wx.requestSubscribeMessage` | 每次调用都弹。**除非勾过「总是保持以上选择」——勾了之后微信直接静默返回 accept，永远不再弹**（这是设计如此，也是整套囤额度策略的基础） |

### 试不出来的三个原因（均已修复）

1. **最致命的**：`habit-edit` 保存成功后立刻 `wx.navigateBack()`，而订阅流程是不等待的旁路。页面被销毁了，弹窗没有宿主，表现就是"什么都没发生"。已改成等订阅流程走完再返回。
2. **失败被静默吞掉**：原来是 `fail: () => resolve(0)`，模板 ID 错、主开关关闭、手势判定失败，全都无声无息。现在会弹框显示 `errCode` + `errMsg`，并写进 Storage `habit_subscribe_last_error`。
3. **调用离用户手势太远**：原来先 `await wx.getSetting` 再弹，多一次异步跳转。微信对 `requestSubscribeMessage` 有"需用户主动触发"的判定，异步链越长越容易被拒。已去掉这一跳（`getSetting` 只保留在静默补额度那条路上）。

### 两个不是 bug、但会让人白试的条件

- 打卡后那条路要求该习惯**填了提醒时间**，没填就什么都不会发生。
- 打卡后是 `setTimeout` 1.5 秒才调，**本身就在手势之外**，很可能直接报 TAP gesture 错误。所以不要用打卡去测。

### 最简单的测试步骤

1. 微信 → 发现 → 小程序 → 长按 habit → **删除**（清掉本地缓存，含引导标记）→ 重新打开。
   *在开发者工具里更快：Storage 面板删掉 `habit_subscribe_guided`。*
2. 新建一个习惯，**务必填提醒时间**，然后保存。
3. 预期依次出现：引导框 → 点「知道了」→ 微信订阅窗（列出打卡提醒 + 空窗预警两个模板）→ **勾「总是保持以上选择」** → 允许。
4. 之后再改提醒保存就不弹了，这是正常的静默 accept。

### 若仍不弹，看错误框里的 errCode

| errCode | 含义 |
| --- | --- |
| `20004` | 用户在微信设置里关闭了该小程序的订阅消息总开关 |
| `20013` / `10001` | 模板 ID 不存在或不合法 |
| `20002` / `20003` | 一次性与长期订阅模板混用（说明 gap 模板申成了长期订阅） |
| 含 `TAP gesture` 字样 | 手势判定失败，需改成显式按钮触发 |

## 二、健身数据已更新（截至 2026-08-14）

### 本次补充

补进 8/12 推日、8/13 臀腿日、8/14 拉日，连带核心、拉伸、晨重共 10 条打卡。当前合计 117 条，最后一天 2026-08-14。

`fitnessProfile` 已重写，纳入以下内容：

- 8/12「停止减脂、先修代谢」四步方案与"纯减脂、零增肌、上肢轻微流失"诊断
- 腰围 -5cm 被对比照否证后的更正（真实变化约 -2 到 -2.5cm）
- 右肩弹响触发条件修正为「手臂过头/高举 + 前臂正握」，而非宽距本身
- 左右臂力量不平衡（右强左弱）及处理原则
- 加重后背阔肌发力感下降 = 代偿，及节奏补强度的对策
- 8/13 实测 1615 kcal 的热量缺口诊断与干预要点
- 史密斯机深蹲定为臀腿主项，以及"杠铃深蹲 / 史密斯机深蹲"的术语约定
- 新成功指标（周期回到 29-31 天、臀围 +1-2cm、臀推稳 15kg×3×12）

### 两个代做的判断（可推翻）

- **8/13 没给「训练后拉伸」打卡**，因为那天只滚了泡沫轴、没做静态拉伸，打了就是假数据。
- **slogan 改成「先把周期养回来，臀围88→90」**，原来的「腰围从85到80」里的 85 已被否证。

### 分块导入用法

`import-chunks/` 下 4 个文件（`chunk-01-of-4.txt` … `chunk-04-of-4.txt`），压缩后总共 35063 字符。

1. 用微信「文件传输助手」把 4 个 txt 发到手机。
2. 打开第 1 个 → 全选复制 → 小程序「我的」→ 从剪贴板导入 → 提示「已收到 1/4」。
3. 回去复制第 2 个 → 再点导入，依此类推。
4. 第 4 块粘完会自动拼装，弹出「习惯 4 个，打卡 117 条」让你确认。

乱序粘也能按块头序号归位，但**不要中途重粘第 1 块**，那会重置缓冲区。

> 更省事的替代：直接在**微信开发者工具的模拟器**里导入。同一个 openid、同一个云环境，电脑上复制 35KB 毫无压力，一次搞定不用分块。建议优先试这条。

### 校验结果

所有改动通过 `node --check`。导入包自查：habitId 无孤儿、同习惯同日期无重复、`createdAt` 全部带 `+08:00`。
