# habit 小程序正式版发布检查清单

> 2026-09-16 制定，替代过时的 habit-pwa-release-checklist.md（PWA 已弃用）。
> 每次发布按顺序走完，勾一项是一项。

## 一、前置条件（全部满足才能提审）

- [ ] **隐私指引已过审且为最新版**：第 1 节含 4 条目——麦克风、剪切板、选中的照片或视频信息、摄像头。提审时平台会自动比对代码隐私接口与指引内容，缺条目会被拦截。查状态：mp 后台 → 设置 → 服务内容声明 → 用户隐私保护指引
- [ ] **AI 链路状态确认**：当前 AI 对话依赖开发者本机中转站（cpolar 穿透）。发布 = 接受"本机离线时 AI 不可用，其他功能不受影响"。若不可接受，先完成中转站上云
- [ ] **体验版已一轮验证**：真机过 AI 对话、语音识别、订阅提醒、打卡、数据导入五项

## 二、版本状态切换（发布核心步骤，容易漏）

- [ ] reminder 云函数环境变量 `MINI_PROGRAM_STATE` = `formal`
- [ ] gapAlert 云函数环境变量 `MINI_PROGRAM_STATE` = `formal`
- [ ] （对照表：开发版 developer / 体验版 trial / 正式版 formal——不切换会导致正式用户收不到订阅消息，消息会被路由到不存在的版本）
- [ ] 两函数切换后**重新部署**使环境变量生效

## 三、数据与配置保护

- [ ] **不覆盖云数据库**：发布流程不涉及任何数据操作；提醒自己不要在控制台误操作 users_data / reminders / subscribe_quota
- [ ] 环境变量完整性核对（云开发控制台逐函数过一遍）：
  - chat：RELAY_BASE_URL / RELAY_INVITE_CODE /（WX_APPID / WX_SECRET 用于内容检测直连）
  - stt：TENCENT_SECRET_ID / TENCENT_SECRET_KEY
  - reminder：REMINDER_TEMPLATE_ID / MINI_PROGRAM_STATE=formal / WX_APPID / WX_SECRET
  - gapAlert：GAP_TEMPLATE_ID / MINI_PROGRAM_STATE=formal / WX_APPID / WX_SECRET
  - security：WX_APPID / WX_SECRET

## 四、代码与提交

- [ ] 本地 git 已全部 push（GitHub 网络抽风时注意补推）
- [ ] app.json `__usePrivacyCheck__` 为 true（严禁带着 false 提审）
- [ ] 无硬编码密钥（密钥只走云函数环境变量）

## 五、提审与发布

- [ ] 开发者工具「上传」→ 填版本号（语义化：0.x.y）+ 备注（本次功能摘要）
- [ ] mp 后台 → 版本管理 → 开发版本 → 提交审核（类目/截图按页面实际填）
- [ ] 审核通过 → 发布
- [ ] 发布后 30 分钟内真机验证：正式版打卡 → 到点等一条订阅提醒（验证 formal 路由正确）

## 六、发布后回滚预案

- 订阅消息不通 → 检查 MINI_PROGRAM_STATE 是否真的为 formal 且已重新部署
- AI 对话不通 → 先查本机中转站与 cpolar 是否在线（地址若变：更新 RELAY_BASE_URL → 重新部署 chat）
- 严重问题 → mp 后台「版本管理」可回退到上一正式版
