# habit 小程序项目问题解答
## 问题1：未提交改动的文件状态与 gapAlert 作用
### 未提交改动的文件状态
当前共有 **19 个已修改文件** + 多个未跟踪文件，其中你提到的核心改动分布：
1.  **云函数改动**：`stt`/`chat`/`reminder`/`saveReminder`/`putData`
2.  **前端页面**：5个核心页面（教练页`assistant.js`、编辑页`habit-edit.js`、首页`index.js`、统计页`stats.js/wxml/wxss`）
3.  **核心工具模块**：`utils/api.js`/`store.js`/`subscribe-config.js`
4.  **全局配置**：`app.json`/`project.config.json`

**状态说明**：这些改动属于**已完成核心修复与功能开发，待提交Git和部署到云开发环境**：
- ✅ `stt`云函数已修复腾讯云ASR的`EngSerViceType`参数缺失问题
- ✅ 前端页面优化了教练页、打卡编辑页、统计页的交互逻辑
- ✅ 新增了订阅额度管理相关的工具模块
- 🔄 所有改动均在本地完成，尚未提交到Git仓库或上传到微信云开发控制台

### gapAlert 云函数作用
这是一个全新的**空窗预警订阅消息云函数**，用于解决用户连续多天未打卡的流失问题：
- 触发逻辑：每天北京时间19:30扫描所有有订阅额度的用户
- 发送规则：仅在连续未打卡**第3天**和**第7天**各发送一次（避免过度骚扰）
- 消息内容：包含上次训练记录、未打卡天数、轻量化回归建议（如「今天做10分钟拉伸也算」）
- 技术细节：通过微信订阅消息模板推送，自动扣减订阅额度，失败时自动处理未订阅/额度耗尽的情况

---
## 问题2：云函数环境变量与火山方舟余额
### 云函数环境变量配置状态
1.  **本地配置**：未在当前`habit-miniprogram`项目根目录找到`.env`文件，历史配置文件位于`D:\软件\cursor\任务文件\habit-cloud-pwa\RJ-habit-cloud-pwa\.env`
2.  **云开发控制台配置**：所有敏感变量需要在微信云开发控制台的云函数环境变量中配置，包括：
   - `TENCENT_SECRET_ID`/`TENCENT_SECRET_KEY`：腾讯云ASR API密钥（用于`stt`语音转文字）
   - `ARK_API_KEY`：火山方舟API密钥（用于`chat`AI对话）
   - `WX_SECRET`：微信小程序AppSecret（用于订阅消息、登录等）
   - `GAP_TEMPLATE_ID`：空窗提醒的微信订阅消息模板ID（用于`gapAlert`）

### 火山方舟账号余额
根据历史记录，你的火山方舟账号曾因**Coding Plan到期+欠费**停用（欠费大概率来自方舟按量推理而非ASR）。目前无法通过本地工具直接查看余额，需要登录[火山方舟控制台](https://console.volcengine.com/ark/)手动查看当前账户状态。

---
## 问题3：云端部署状态
### 已部署过的云函数
- `chat`/`reminder`/`saveReminder`/`putData`：属于历史已部署的云函数，当前有本地改动但未重新上传

### 未部署/待重新部署
- `stt`：本地已完成参数修复，但未重新上传部署到云开发
- `gapAlert`/`security`/`subscribeQuota`：全新的未跟踪云函数，仅存在于本地，从未部署过

### 小程序客户端代码
所有前端页面、工具模块的改动都需要在微信开发者工具中**上传代码**才能同步到云端，目前仅修改了本地文件，未生效。

---
## 问题4：当前开发版小程序运行状态
### 核心链路情况
1.  **打卡链路**：前端页面和工具模块已完成修改，理论上可以正常运行，只要重新部署`putData`/`saveReminder`等云函数后即可正常使用打卡、训练记录功能。
2.  **AI对话链路**：依赖`chat`云函数和火山方舟API，若未重新部署`chat`云函数、未配置`ARK_API_KEY`，或火山方舟账号仍欠费，则AI对话会失败。
3.  **语音转文字链路**：已修复腾讯云ASR的参数缺失问题，只要重新部署`stt`云函数并配置`TENCENT_SECRET_ID`/`TENCENT_SECRET_KEY`，即可正常使用麦克风授权和语音识别功能。

### 已知未完成/可能失败的功能
- 空窗提醒（`gapAlert`）：全新云函数未部署，无法触发
- 订阅消息：未配置`GAP_TEMPLATE_ID`等模板变量
- 部分工具模块：`utils/subscribe.js`未完成完整测试

---
## 问题5：AI教练头像定稿状态
目前仍沿用 **2026-08-26的推荐排序**：
1.  **AF方案（推荐）**：墨黑底+黄绿双环，40px分辨率不模糊，与品牌名「圈圈」强绑定
2.  **AJ方案**：靛紫渐变+白黄同心环，与PWA现用头像风格一致
3.  旧方案AA：大字教字样，视觉辨识度稍弱

你可以通过预览页确认最终选择：`D:\软件\cursor\任务文件\habit-miniprogram\icon\preview-quanquan.html`，该页面包含13个可选方案的对比。

---
## 问题6：env.txt 和 data/cloud-data.json 实际位置
根据历史记录和当前目录结构：
1.  **env.txt/.env文件**：未在当前`habit-miniprogram`项目中找到，历史路径为`D:\软件\cursor\任务文件\habit-cloud-pwa\RJ-habit-cloud-pwa\.env`，如果需要在小程序项目中使用，请将该文件复制到`habit-miniprogram`根目录，或在云开发控制台中配置环境变量。
2.  **data/cloud-data.json**：同样未在当前`habit-miniprogram`目录中找到，历史路径为`D:\软件\cursor\任务文件\habit-cloud-pwa\RJ-habit-cloud-pwa\data\cloud-data.json`，包含`admin/test1/test2`三个账号的完整用户数据。如果需要导入测试数据，请将该文件上传到微信云开发的数据库中。