> **本文件方向已作废（勿据此开发）。** 文中 key 粒度、OpenAI 兼容、配额限流、看板放哪等"待补全"清单，其实都已在 **WorkTrace Relay 独立服务**里实现，不在 habit 仓库。正确认知请看同目录 `AI中转站统一定位说明.md`。

# habit小程序AI中转站开发补全与解答指南（历史归档，内容作废）
## 一、项目概述
本项目是为habit微信小程序开发的AI服务调用中转站，用于解决原有AI对话功能硬编码绑定火山方舟API的问题，实现统一的AI服务调用入口，支持多服务商动态切换、配置化管理、熔断降级、用量统计等功能，提升系统的灵活性、稳定性和可扩展性。

## 二、当前开发状态
### 1. 文档已覆盖（地基功能）
| 功能项 | 详细说明 |
|-------|----------|
| 统一调用入口（重构chat云函数） | 已完成chat云函数的重构，将原有硬编码的火山方舟调用提取为通用调用层，兼容现有小程序前端的`sendChat`调用链路，无需修改前端代码即可切换到新架构 |
| 配置层、多服务商适配 | 已完成配置层抽象，支持通过云函数环境变量读取AI服务商、模型、密钥等配置，预留了多服务商适配接口，火山方舟适配已完成并兼容原有逻辑 |
| 熔断降级、监控日志 | 已实现基础的调用异常处理和日志记录功能，可扩展熔断降级逻辑，记录调用耗时、错误信息等关键数据 |
| 部署与测试方案 | 已完成微信云开发环境的部署流程和测试方案文档，支持本地调试和线上部署 |

### 2. 文档未提（网关主体功能，接手后需补全）
| 功能项 | 详细说明 |
|-------|----------|
| key 签发/校验/停用（按工具生成key） | 为每个接入的工具生成独立API密钥，用于身份鉴权，支持按工具维度管理密钥，包括创建、校验、停用等操作 |
| 用量计量（每次调用记key、工具、token数） | 记录每次AI调用的详细信息：密钥ID、接入工具、使用者、请求参数、token消耗数量、调用时间、响应结果等，为后续统计提供数据基础 |
| 统计聚合与看板（按key/工具查用量） | 对调用数据进行统计聚合，支持按密钥、工具、时间维度查询用量统计数据，生成可视化看板展示调用量、token消耗、成功率等指标 |
| 对外调用通道（工具不是小程序前端，怎么调进来） | 提供非小程序前端的调用方式，如HTTP API接口、SDK封装，支持外部工具、第三方服务接入AI中转站服务 |

## 三、四个核心问题解答
### 问题1：key粒度：一个工具一个key就够，还是要“工具+使用者”两级（能看出某个工具里是谁用的）？
#### 推荐方案：优先采用「工具+使用者」两级粒度，单用户场景可简化为工具级
##### 详细分析：
1.  **两级粒度的优势**：
    - 权限隔离更精细：可以区分同一个工具下不同使用者的调用情况，适合团队版多用户使用的habit小程序
    - 用量统计更精准：可以按使用者维度统计token消耗，方便做用户级的配额管理和费用分摊
    - 安全可控性更强：如果某个使用者的密钥泄露，可以单独停用该密钥，不影响其他使用者的使用
2.  **单用户场景简化方案**：
    如果当前是个人使用的单用户场景，可以先实现「工具级」密钥，快速上线功能，后续再扩展为两级粒度，避免过度设计增加开发成本

### 问题2：接口形态：工具都是你自己写的，接口可以随便定义；但要不要顺手做成OpenAI兼容格式？好处是以后想接Cursor之类现成工具零成本。
#### 推荐方案：同时支持原有兼容接口和OpenAI兼容格式，兼顾现有业务和未来生态
##### 详细分析：
1.  **OpenAI兼容格式的优势**：
    - 零成本接入现有工具：Cursor、ChatGPT等IDE插件和AI工具原生支持OpenAI格式的API，无需额外开发适配代码即可快速接入
    - 生态兼容性强：后续可以快速接入其他支持OpenAI格式的AI客户端、第三方服务，提升系统的扩展性
    - 开发成本低：现有重构的通用调用层可以快速映射到OpenAI格式的接口定义，无需大量修改现有代码
2.  **具体实现方案**：
    - 保留原有小程序前端的`sendChat`调用接口，保持现有业务逻辑不变
    - 新增`/v1/chat/completions`标准OpenAI格式接口，兼容OpenAI的请求参数（`model`、`messages`、`temperature`、`max_tokens`等）和响应格式
    - 两种接口共享底层的AI调用逻辑，实现代码复用

### 问题3：配额：目前是纯监控记账，还是要能限流（比如某key每天限N次/限token，超了拒掉或降级）？
#### 推荐方案：分阶段实现，先做纯监控记账，后续逐步增加限流降级能力
##### 详细分析：
1.  **阶段1：纯监控记账（当前优先）**
    - 仅记录每次调用的关键信息，不做强制限制
    - 优势：可以快速上线，验证业务场景，收集真实的调用数据，了解用户的使用习惯和用量分布
    - 适用场景：项目初期，快速验证中转站架构的可行性
2.  **阶段2：限流降级（后续迭代）**
    - 支持按密钥配置配额：每日调用次数限制、每日token消耗限制、每分钟调用频率限制
    - 支持超配额后的两种处理策略：
      1.  **直接拒绝**：返回`429 Too Many Requests`错误码，提示用户配额不足
      2.  **降级处理**：切换到免费/低性能模型，或者返回友好提示信息，避免用户完全无法使用
    - 适用场景：业务稳定后，需要控制成本、保障服务稳定性的场景

### 问题4：看板放哪：查用量的管理界面，是做在habit小程序里加个管理页，还是独立Web页？
#### 推荐方案：优先开发小程序内管理页，预留独立Web页扩展空间
##### 详细分析：
1.  **小程序内管理页（优先方案）**
    - **优势**：
      1.  无需额外开发独立应用，复用现有小程序的用户认证和权限体系，开发成本低
      2.  用户可以直接在小程序内查看自己的用量统计，无需跳转，使用体验更流畅
      3.  符合现有用户的使用习惯，无需学习新的平台
    - **适用场景**：个人用户、单团队用户的用量查询
2.  **独立Web管理页（扩展方案）**
    - **优势**：
      1.  功能更丰富，支持更复杂的统计分析和可视化图表，适合团队/企业级用户
      2.  可以支持多账号管理、团队用量统计、费用报表导出等高级功能
      3.  桌面端操作更方便，适合管理员使用
    - **适用场景**：团队版、企业版的用量管理
3.  **具体实现**：
    - 先在habit小程序中新增「用量管理」页面，展示当前用户的调用统计、token消耗等基础信息
    - 预留独立Web页的API接口，后续可以基于这些接口开发独立的管理后台
    - 管理员账号可以查看所有用户的用量统计，支持按工具、使用者维度筛选数据