# habit 微信小程序项目交接文档

> 最后更新：2026-09-10。本版基于代码实测核验，与历史版本（2026-08-31）相比，完成状态、上游架构、隐私合规均已更新。

## 一、项目概述

### 核心定位

健身闭环工具，包含 AI 教练助手（已接 DeepSeek）、健身数据跟踪、训练计划/记录、订阅消息提醒（打卡提醒 + 空窗召回）等功能。教练人格支持 normal / mild / sassy（毒舌）三档语气，由用户在设置中切换。

### 技术栈

- 前端：微信小程序原生开发（uni-app 未使用，直接原生）
- 后端：微信云开发（云函数、云数据库、云存储）
- AI 上游：**WorkTrace Relay 中转站**（本地 Python FastAPI + cpolar 内网穿透）→ DeepSeek
- 语音识别：**腾讯云一句话识别 ASR**（`tencentcloud-sdk-nodejs-asr`，一句话识别 SentenceRecognition）
- **PWA 端已弃用**：小程序是按 PWA 功能重做的完整实现，无迁移需求

### 项目目录结构（核心路径）

```
D:\软件\cursor\任务文件\habit-miniprogram\
├── app.js/json/wxss          # 小程序全局配置（__usePrivacyCheck__: true）
├── pages/                    # index今日/calendar日历/assistant教练/analytics分析/stats我的/habit-edit/habit-detail/privacy
├── custom-tab-bar/           # 自定义底部导航栏
├── cloudfunctions/           # 12 个云函数（详见下表）
├── icon/                     # AI 教练头像资源（AM 方案已定稿）+ preview-quanquan.html 方案对比页
├── import-chunks/            # 健身数据分块导入包（当前 6 块）
├── smoke/                    # 测试脚本：test-callrelay.js（中转站集成测试）等
├── utils/                    # api.js / store.js / subscribe.js / subscribe-config.js / constants.js
├── 数据导入格式.md            # 导入 JSON 的完整格式规范
└── 订阅弹窗测试与数据导入说明.md
```

### 云函数清单（12 个）

| 云函数 | 功能 | 部署状态 | 备注 |
|---|---|---|---|
| chat | AI 对话（调中转站） | ✅ 已部署 | 环境变量 RELAY_BASE_URL / RELAY_INVITE_CODE 已配 |
| stt | 语音转文字（腾讯云ASR） | ✅ 已部署 | 环境变量 TENCENT_SECRET_ID / TENCENT_SECRET_KEY 已配 |
| reminder | 打卡提醒（每小时整点定时器） | ✅ 已部署 | 环境变量 REMINDER_TEMPLATE_ID 已配 |
| gapAlert | 空窗召回（每天 19:30 定时器） | ✅ 已部署 | 环境变量 GAP_TEMPLATE_ID 已配，2026-09-10 上线 |
| security | 头像图片安全检测 | ✅ 已部署 | checkImage action |
| subscribeQuota | 订阅额度记账 | ✅ 已部署 | refill / 查询 action |
| saveChat / getChat | 聊天记录存取 | ✅ 已部署 | |
| saveReminder / putData / getData | 提醒与数据存取 | ✅ 已部署 | |

## 二、AI 链路架构（当前生效）

```
小程序前端 ──wx.cloud.callFunction──▶ chat 云函数
                                        │ POST {RELAY_BASE_URL}/api/v3/chat/completions
                                        │ 请求头 X-Invite-Code（邀请码，非直连密钥）
                                        ▼
                              WorkTrace Relay 中转站（D:\软件\cursor\任务文件\worktrace-relay）
                                        │ 本地 127.0.0.1:8000，cpolar 穿透公网
                                        ▼
                                    DeepSeek（deepseek-chat）
```

- **邀请码只放云函数环境变量**（RELAY_INVITE_CODE），严禁写入前端——小程序包可被反编译
- **中转站部署决策（2026-09-10）**：维持本地 + cpolar，不上云。cpolar 客户端不重启则公网地址不变；**地址变了需更新 chat 的 RELAY_BASE_URL 并重新部署 chat**
- 中转站本体在另一个专门任务中改造，habit 侧只消费 RELAY_BASE_URL，不碰 relay 代码
- 中转站本地集成测试：`cd smoke && RELAY_BASE_URL=http://127.0.0.1:8000 RELAY_INVITE_CODE=HB-xxx node test-callrelay.js`

## 三、已完成的关键修复（历史问题存档）

| 问题 | 最终解法 | 状态 |
|---|---|---|
| 火山 ASR 欠费停用 | stt 迁移腾讯云一句话识别 | ✅ 2026-09-09 真机验证通过 |
| 腾讯云 ASR 参数连环报错（6 轮） | 最终参数集：`SourceType: 1`（数据在 post body，0 是 URL 模式）、`VoiceFormat: 'mp3'`（字符串格式名）、`FilterModal: 1`（int64）、`DataLen: buffer.length`；一句话识别**没有** EngineModelType 参数 | ✅ 已修复（提交 4fca48c/bffb3cd/eb723f6/823d327/4fa2d05） |
| 火山方舟 chat 上游欠费 | chat 切 WorkTrace Relay 中转站，移除全部 ARK_* 依赖 | ✅ 2026-09-08 全链路验证通过 |
| 教练页输入栏高度 | 隐藏 text 代理尺子 + SelectorQuery 实测（ISSUE-输入栏高度.md 有完整方案） | ✅ 2026-08-07 已解决 |
| 隐私指引缺剪贴板声明 | getClipboardData 报 "api scope is not declared"；mp 后台补声明 | ✅ 2026-09-10 审核通过 |
| 隐私指引整体生效规则 | 指引是整份重审非增量，**麦克风+剪贴板必须同时在**，缺失条目功能被拦 | ✅ 当前两条款齐全 |

## 四、订阅消息体系

- 模板已申请（mp 后台「我的模板」），通道配置在 `utils/subscribe-config.js`：
  - `reminder`：打卡提醒 → 模板 ID `Cy8rFD7AFyrWhXYsvLu4ck8fOdzm0Jn6zMAyblY1WVM`
  - `gap`：空窗召回 → 模板 ID `gl3NYmWBhC_e5nWy8hSLCoAoMHDsrSXaw9HNTohbPC4`
- 额度模型：用户每同意一次订阅 = 1 条下发额度，单通道上限 5（QUOTA_CAP），记账在云数据库 subscribe_quota 集合（subscribeQuota 云函数）
- 触发点：保存带提醒时间的习惯（habit-edit）→ 弹引导 → 弹微信授权窗（引导勾选"总是保持以上选择"）；「我的」页静默补充额度
- 定时器：reminder 每小时整点检查（下发条件：到点未打卡 + 有额度）；gapAlert 每天 19:30（连续 3/7 天未打卡，单通道发一次，防骚扰）

## 五、数据导入体系

- **导入是整包覆盖**（habits/checkins/chatHistory 全量替换），必须先「我的 → 导出数据」取基底再追加
- 大数据包走**分块粘贴**：`import-chunks/` 下带 `HABIT i/n` 头的 txt，顺序或乱序粘均可（重粘第 1 块会重置缓冲区）；改数据后 `node smoke/make-import-chunks.js 9000` 重新生成
- **剪贴板导入依赖隐私声明**（已声明）；备选方案：wx.chooseMessageFile 从微信聊天选文件（无需声明，尚未实现）
- 2026-09-09 已完成 82KB 健身数据导入（6/11-9/9 历史，习惯 4 个打卡 163 条），云库+真机验证一致
- 体重/围度等数据只进 `settings.fitnessProfile`（纯文本分节，AI 教练每次对话读取），无结构化字段

## 六、Git 与发布

- 仓库：`github.com/adsgtdyj/habit`，分支 `miniprogram`，**已与远端同步**（2026-09-10 push，至 3747f1d 及后续）
- 提交纪律：改动后即时 commit，密钥永不入库（环境变量走云开发控制台）
- 发布检查清单：`habit-pwa-release-checklist.md`
- **env.txt 与 data/cloud-data.json 不在本项目内**，历史路径：`D:\软件\cursor\任务文件\habit-cloud-pwa\RJ-habit-cloud-pwa\`（PWA 已弃用，仅存档参考）
- 头像：AM 方案已定稿并上传公众号平台（以平台为准，md 文档不作数）

## 七、注意事项（接手必读）

1. **多 AI 协作核验**：本项目先后经多个 AI 助手协作，接手时先读交接文档再**用代码核实**前一位的声明（历史上有过声明与现状不符的教训）
2. **涉及 worktrace-relay 的改动需先与用户确认**——它是独立项目，habit 侧不碰
3. 隐私指引更新是整份重审：增删任何信息类型前，先确认现有条目（麦克风+剪贴板）不会丢
4. 云函数部署后若行为异常，先查环境变量是否配置（控制台 → 云函数 → 配置）
5. 微信云开发按量计费：云函数出网请求不受小程序域名校验限制
6. stt 的 6 轮参数报错教训：腾讯云参数类型校验逐个报错，改一处测一次，不要一次改多个
