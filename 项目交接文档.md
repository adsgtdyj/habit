# habit 微信小程序项目交接文档
## 一、项目概述
### 核心定位
健身闭环工具，包含AI教练助手、健身数据跟踪、训练计划/记录、饮食分析等功能，原MVP设计为AI偏毒舌风格，提醒频率为设定时间一次+晚9点汇总。
### 技术栈
- 前端：微信小程序原生开发
- 后端：微信云开发（云函数、云存储）
- 第三方服务：腾讯云一句话识别（ASR语音转文字）、火山方舟（原计划，现仅部分云函数chat可能仍在使用）
### 项目目录结构（核心路径）
```
D:\软件\cursor\任务文件\habit-miniprogram\
├── app.js/json/wxss          # 小程序全局配置
├── pages/                    # 页面文件
├── custom-tab-bar/           # 自定义底部导航栏
├── cloudfunctions/           # 微信云函数
│   ├── stt/                  # ✅ 语音转文字（已修复核心问题）
│   ├── chat/                 # AI对话云函数（调用火山方舟）
│   ├── saveChat/             # 保存聊天记录
│   ├── getChat/              # 获取聊天记录
│   ├── reminder/             # 提醒功能
│   ├── saveReminder/         # 保存提醒
│   ├── security/             # 安全相关
│   ├── subscribeQuota/       # 订阅额度管理
│   └── 其他云函数...
├── icon/                     # AI教练头像设计资源
│   └── preview-quanquan.html  # 头像方案对比预览页
├── utils/                    # 工具函数
└── 文档文件/
    ├── 订阅弹窗测试与数据导入说明.md
    ├── 数据导入格式.md
    └── ISSUE-输入栏高度.md
```

## 二、历史问题与已完成解决方案
| 问题类型                | 问题描述                                                                 | 解决方案                                                                 | 状态       |
|-------------------------|--------------------------------------------------------------------------|--------------------------------------------------------------------------|------------|
| ASR服务停用             | 原火山ASR服务因用户Coding Plan到期+欠费无法使用，替换为腾讯云ASR          | 迁移云函数stt，使用`tencentcloud-sdk-nodejs-asr`替代火山WebSocket协议       | ✅ 已完成   |
| 麦克风权限报错          | 首次使用时弹出errno 112（scope未声明），授权弹窗异常                     | 小程序后台声明「麦克风」权限并提交审核，`app.json`配置`__usePrivacyCheck__: true` | ✅ 已解决   |
| ASR API参数缺失         | 腾讯云ASR接口报错`The request is missing the required parameter 'EngSerViceType'` | 补全必填参数：`ProjectId: 0`、`SubServiceType: 0`、`EngSerViceType: '16k_zh'` | ✅ 已修复   |
| 隐私协议审核            | 首次使用麦克风需用户授权，隐私协议未通过会导致功能异常                   | 完成隐私协议审核，已在小程序后台配置权限声明                               | ✅ 已通过   |
| 语音交互体验            | 首次授权弹窗后首次长按录音会被打断，第二次正常（微信官方预期行为）         | 无需代码修改，可在文档中说明该交互特性                                     | 📝 待优化   |

## 三、当前代码与配置现状
### 1. 核心修复模块：stt云函数
- **路径**：`cloudfunctions/stt/index.js`
- **功能**：接收前端上传的语音文件`fileID`，下载后调用腾讯云一句话识别API，返回识别文本
- **当前代码**：已补全所有必填参数，正确调用腾讯云ASR接口
- **依赖要求**：需在云函数`package.json`中添加`tencentcloud-sdk-nodejs-asr`依赖
- **环境变量**：`TENCENT_SECRET_ID`、`TENCENT_SECRET_KEY`（腾讯云API密钥）

### 2. 全局配置
- **小程序AppID**：`wx80ae23bfa51c5348`
- **隐私配置**：`app.json`已开启`__usePrivacyCheck__: true`，自动处理隐私授权弹窗
- **自定义TabBar**：已实现`custom-tab-bar`组件，适配小程序导航需求
- **设计标准**：遵循`habit V13设计标准`（手机优先、游戏化、教练人格、强视觉）

### 3. 未完成的UI设计
- **AI教练头像**：正在设计「圈圈」形象，当前推荐方案：
  - AF方案：墨黑底+黄绿双环，40px分辨率不模糊，与品牌名强绑定
  - 预览页：`icon/preview-quanquan.html`，包含13个方案对比
  - 已验证免费商用字体：站酷快乐体、马善政毛笔楷、龙藏硬笔行草（避开了有版权风险的系统字体）

## 四、待办事项（按优先级排序）
### 高优先级
1. **验证stt云函数**：真机测试语音转文字功能，确认麦克风授权正常、识别结果正确
2. **定稿AI教练头像**：从AF/AJ方案中选择最终头像，替换小程序配置中的图标资源
3. **部署云函数**：重新部署所有更新后的云函数（尤其是`stt`和`chat`）
4. **发布小程序版本**：按照`habit PWA发布检查清单`的流程，注意不要覆盖`env.txt`和`data/cloud-data.json`

### 中优先级
1. **修复输入栏高度问题**：处理`ISSUE-输入栏高度.md`中记录的输入框高度适配问题
2. **优化语音交互**：针对小程序端的长按语音、上滑取消交互做适配优化（参考PWA端的pointer capture方案）
3. **配置云开发环境变量**：确保`TENCENT_SECRET_ID`、`TENCENT_SECRET_KEY`、`WX_SECRET`等密钥正确配置
4. **测试AI对话功能**：验证`chat`云函数调用火山方舟API的可用性

### 低优先级
1. **发布多账号功能**：完成多账号模块的发布准备（已实现代码，待发布）
2. **完善订阅消息功能**：验证订阅消息的额度管理、模板推送逻辑（参考`habit 订阅额度架构`文档）
3. **迁移PWA未发布改动**：将PWA端的多账号、语音输入等8项改动适配到小程序端（当前优先级较低）

## 五、关键参考文档与资源
1. **设计规范**：`project-habit-v13-design-standards.md`（V13大重构要求：手机优先、游戏化等）
2. **隐私配置**：`project-habit-miniprogram-cloudbase-migration.md`（云开发迁移与隐私权限配置）
3. **ASR实现细节**：`habit-miniprogram-stt.md`（语音转文字的技术方案、协议细节）
4. **发布流程**：`habit-pwa-release-checklist.md`（小程序发布前的检查清单）
5. **历史数据**：`data/cloud-data.json`（包含admin/test1/test2三个账号的完整用户数据，发布时请勿覆盖）

## 六、注意事项
1. **云函数依赖**：所有云函数需在微信云开发控制台正确配置依赖包，`stt`云函数需添加`tencentcloud-sdk-nodejs-asr`
2. **环境变量**：敏感密钥（如腾讯云API密钥、微信AppSecret）请勿硬编码，需通过云开发环境变量配置
3. **隐私合规**：所有涉及用户权限的功能（如麦克风）必须在小程序后台完成权限声明并审核通过
4. **欠费风险**：避免使用按量付费且无额度限制的服务（如原火山ASR），优先使用有免费额度或可关闭后付费的服务
5. **多主体限制**：微信同声传译插件不支持个人主体小程序，已排除该方案，当前使用`wx.getRecorderManager()`原生录音API